
name: Test

on: [push]

jobs:
  test_gentoo_build:
    runs-on: ubuntu-18.04
    name: Build on gentoo aarch64
    steps:
      - uses: actions/checkout@v2.1.0
      
      - uses: expeditioneer/run-on-arch-action@master
        name: Run commands
        id: runcmd
        with:
          arch: aarch64
          distro: gentoo
          
          run: |
            echo "***********************************************************************"
            echo "/etc/portage/make.conf:" 
            cat /etc/portage/make.conf
            echo "***********************************************************************"
            echo "gcc -v"
            gcc -v
            echo "***********************************************************************"
